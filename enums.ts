export enum COLUMNS {
    YEAR,
    MONTH,
    DATE,
    TITLE,
    DESCRIPTION,
    PRICE,
    PAYER,
    __LENGTH,
}

export enum META_COLUMNS {
    TYPE,
    KEY,
    VALUE,
    __LENGTH,
}

export enum META_TYPE {
    TITLE = 'title',
    ACCOUNT = 'account',
}
